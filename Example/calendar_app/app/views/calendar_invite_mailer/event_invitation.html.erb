<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body style="font-family: Arial, sans-serif; line-height: 1.6; color: #333;">
    <div style="max-width: 600px; margin: 0 auto; padding: 20px;">
      <h1 style="color: #1a1a1a;"><%= @event.title %></h1>
      <div style="background-color: #f8f9fa; padding: 15px; border-radius: 5px; margin: 20px 0;">
        <p><strong>Date:</strong> <%= @event.start_time.in_time_zone(@event.timezone).strftime("%B %d, %Y") %></p>
        <p><strong>Time:</strong> <%= @event.start_time.in_time_zone(@event.timezone).strftime("%I:%M %p") %> - <%= @event.end_time.in_time_zone(@event.timezone).strftime("%I:%M %p") %> PT</p>
        <p><strong>Location:</strong> <%= @event.location %></p>
      </div>
      <div style="margin: 20px 0;">
        <%= simple_format(@event.description) %>
      </div>
      <% if @event.notes.present? %>
        <div style="margin: 20px 0; padding: 15px; border-left: 4px solid #007AFF;">
          <strong>Additional Notes:</strong><br>
          <%= @event.notes %>
        </div>
      <% end %>
      <p style="margin-top: 30px; color: #666;">
        A calendar invitation has been attached to this email. Please add it to your calendar to receive updates and reminders.
      </p>
    </div>
  </body>
</html>